# Smoke test

- Endpoint: `GET /health`
- Kết quả mong đợi: `{"status":"ok"}`
- Chạy bằng: `npm test`

Ghi chú: đây là minh chứng bằng văn bản cho buổi 2; nếu cần ảnh chụp màn hình, chụp terminal khi chạy `npm test` và lưu trong `docs/`.
